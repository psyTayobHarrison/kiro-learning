# Budget Tracker

A full-stack expense tracking application. Users can create, read, update, and delete expenses, filter by category, and view spending summaries grouped by category.

- No authentication or user accounts
- No pagination
- Single-page Angular frontend consuming a Spring Boot REST API
- PostgreSQL for persistence
- API base path: `/expenses`
